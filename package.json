{
  "scripts": {
    "serve": "bundle exec jekyll serve",
    "simpleServer": "npx http-server -c-1 .",
    "transpile": "npx tsc"
  },
  "private": true,
  "devDependencies": {
    "@types/flickity": "^2.2.3",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
